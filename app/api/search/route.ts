import { NextRequest, NextResponse } from 'next/server'; import { searchNews } from '@/lib/supabase'; export async function GET(req: NextRequest) { try { const q = req.nextUrl.searchParams.get('q'); if (!q) return NextResponse.json({error: 'Missing query'}, {status: 400}); const data = await searchNews(q, 50); return NextResponse.json({success: true, data}); } catch (e) { return NextResponse.json({error: String(e)}, {status: 500}); } }
