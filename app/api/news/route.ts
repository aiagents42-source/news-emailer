import { NextRequest, NextResponse } from 'next/server'; import { getNewsByCategory, getLatestNews } from '@/lib/supabase'; export async function GET(req: NextRequest) { try { const cat = req.nextUrl.searchParams.get('category'); const limit = Math.min(parseInt(req.nextUrl.searchParams.get('limit') || '10'), 100); const data = cat ? await getNewsByCategory(cat, limit) : await getLatestNews(limit); return NextResponse.json({success: true, data}); } catch (e) { return NextResponse.json({error: String(e)}, {status: 500}); } }
