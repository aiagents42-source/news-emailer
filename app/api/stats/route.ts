import { NextRequest, NextResponse } from 'next/server'; import { getNewsStats } from '@/lib/supabase'; export async function GET() { try { const stats = await getNewsStats(); return NextResponse.json({success: true, data: stats}); } catch (e) { return NextResponse.json({error: String(e)}, {status: 500}); } }
